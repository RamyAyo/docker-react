language: generic
sudo: required
services:
    - docker
    
before_install:
    - docker build -t ramyayoub/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ramyayoub/docker-react npm run test
    